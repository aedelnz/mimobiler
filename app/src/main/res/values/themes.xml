<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Mimobiler" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Mimobiler" parent="Base.Theme.Mimobiler" />

    <style name="Theme.NoTitleBar.Fullscreen" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>